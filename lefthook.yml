pre-commit:
  parallel: true
  commands:
    lint:
      runner: pwsh -NoLogo -NonInteractive -Command
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      exclude: "**/node_modules/**"
